module Rules where

-- type Player1Token Char
-- type Player2Token = 'y'
-- type EmptySpace   = '-'
--
-- type Space Player1Token || Player2Token || EmptySpace

add :: (Num a) => a -> a
add x = x + 1
